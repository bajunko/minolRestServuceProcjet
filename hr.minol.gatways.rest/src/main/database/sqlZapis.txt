create table multimedija.ZAPIS(
    ID  serial PRIMARY KEY,
    TEXT character varying(100),
	SLIKA bytea,
	GEOLOAKCIJA character varying(100),
	VIDEO character varying(100),
	ZVUK character varying(100),
    USERNAME character varying(100),
	VRIJEMEUNOSA bigserial
	
);

		
		create table multimedija.USERS(


		ID serial,
		
		USERNAME character varying(100),
		PASSWORD character varying(100),
		
		 PRIMARY KEY (ID)		
		);
		
		
		create table multimedija.FOLDER(
    ID  serial PRIMARY KEY,
    NAZIV character varying(100)
    
	
	
);


create table multimedija.FILE(
    ID  serial PRIMARY KEY,
    NAME character varying(100),
        contenttype character varying(100),
        contentsize bigserial,
        link character varying
    
);


create table multimedija.NEWS(
    ID  serial PRIMARY KEY,
    TITLE character varying,
        BODY character varying,
        CATEGORY character varying,
        DATE bigserial,
        GPSCOORDINATES character varying,
        PHOTOLINKS character varying,
        VIDEOLINKS character varying,
        AUDIOLINKS character varying,
        ISHIDDEN bool
);
		
	